@tailwind base;
@tailwind components;
@tailwind utilities;

/* Glassmorphism Design */
.glass-card {
  @apply bg-white/30 backdrop-blur-md border border-white/40 shadow-lg rounded-xl p-6;
}

.glass-item {
  @apply p-5 bg-white/50 backdrop-blur-sm border border-white/30 rounded-xl shadow hover:scale-105 transition-all duration-300 animate-fade-in;
}

.btn-primary {
  @apply inline-block mt-2 px-4 py-2 rounded bg-blue-600 text-white text-sm hover:bg-blue-700 transition;
}